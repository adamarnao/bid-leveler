import AppShell from "@/components/layout/AppShell";

export default function BudgetsPage() {
  return (
    <AppShell title="Budgets">
      <section className="project-setup-placeholder">
        <strong>Future module</strong>
        <p>
          Historical cost database and budgeting tools will connect here after
          real takeoffs, allowances, bid totals, and pricing history are
          available.
        </p>
      </section>
    </AppShell>
  );
}
