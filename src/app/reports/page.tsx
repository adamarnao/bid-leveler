import AppShell from "@/components/layout/AppShell";

export default function ReportsPage() {
  return (
    <AppShell title="Reports">
      <section className="project-setup-placeholder">
        <strong>Future module</strong>
        <p>
          Reports and analytics will connect here after the core project, bid,
          invite, and leveling workflows have stable production data.
        </p>
      </section>
    </AppShell>
  );
}
